# 數位塔位 / 主權數位先祖系統 架構文件

> **DSAS — Data Sovereignty as Soul**
> 一個結合區塊鏈與生成式 AI 的數位永生服務,讓家屬透過 NFT 擁有逝者的數位分身,並能與其互動(對話、影像、聲音)。

---

## 一、核心理念

在 Web2 時代,記憶是「租來」的——存於 Google、Meta 的伺服器,平台倒了記憶就消失。
在 Web3 時代,透過 ERC-721、ERC-6150 與 Arweave,我們第一次實現「記憶的私有制」。

**核心承諾**:即使平台倒閉,只要 Arweave 還在運行、家屬持有 NFT 錢包,任何人都能用 TXID 把資料抓回來重現逝者。

---

## 二、三層架構總覽

| 層級 | 技術 | 角色 |
|---|---|---|
| 身分層 | ERC-721 + ERC-6150 | 數位塔位的所有權證明、家族層級 |
| 儲存層 | Arweave + Irys | 永久儲存照片、影片、音檔、文字 |
| 運算層 | LoRA + RAG + 聲音/影像生成 | 即時重現逝者的外貌、聲音、人格 |

---

## 三、身分層:NFT 設計

### 3.1 採用標準

- **ERC-721**:每位逝者 = 一張獨一無二的「數位塔位」NFT
- **ERC-6150 (Hierarchical NFTs)**:處理家族層級結構

### 3.2 為何不用 ERC-6551

ERC-6551 是讓 NFT 本身擁有錢包(可以持有資產、簽交易)。在本系統中:

- 逝者是「被緬懷的對象」,不是行為主體,不需要主動簽交易
- 一般 EOA 錢包(MetaMask、Rabby 等)本來就能持有任意數量的 ERC-721,家屬不需要 TBA 來「裝」多個塔位
- 少一層合約 = 更省 gas、更低風險、更簡單的 UX

**未來保留升級空間**:若要做祭祀基金、回憶資產化、跨代繼承容器,ERC-6551 可隨時加掛在現有 ERC-721 上,不破壞既有架構。

### 3.3 ERC-6150 的家族層級

類似資料夾路徑的層級結構:

- 根 NFT(始祖) → 子 NFT(第一代) → 孫 NFT(第二代) → ⋯⋯
- 完美呼應東方文化的「宗祠」邏輯
- 鏈上即家譜,不需要外部資料庫維護家族關係

### 3.4 鑄造權限模型

合約初始化時定義 `MINTER_ROLE`,可選三種模式:

1. **絕對控制模式**:只有當前 NFT 持有者能為後代開位
   - 優:私密性高
   - 缺:持有者消失,支線就斷

2. **白名單 / 平台輔助模式**:持有者可預設「繼承人」或「管理員」
   - 持有者過世後,平台或家族委員會可協助驗證身份並鑄造

3. **自動觸發模式**:設定鏈上條件(如持有特定家族代幣),條件符合者可自發領取

### 3.5 持有方式

家屬使用一般 EOA 錢包(MetaMask、Rabby、Phantom 等)持有塔位 NFT。一個錢包可同時持有多張塔位(爺爺、奶奶、外公、外婆⋯⋯)。轉移、繼承走標準 ERC-721 的 `transferFrom`。

---

## 四、儲存層:Arweave 永久儲存

### 4.1 為何選 Arweave

Arweave 是一個 Layer 1 區塊鏈,專為「永久儲存」設計。透過 AR 代幣激勵礦工持續保存資料,用戶只需**一次性付費**,資料就永久存在於去中心化網路。被稱為「數據的比特幣」。

### 4.2 分層儲存策略

**小型資料 → 鏈上 Metadata / Arweave Tags**

- 內容:姓名、生卒年、生平簡介、指向大型檔案的 TXID
- 位置:NFT 的 `tokenURI` 或 Arweave 交易的 Tags
- 目的:前端秒讀,類似塔位外的名牌,家屬一滑手機就能看到照片縮圖與名字

**大型資料 → Arweave Permaweb**

- 內容:原畫質照片、長影片、生前著作、錄音檔
- 上傳:透過 Irys(原 Bundlr)切片打包,支援 >200MB 大檔案高速上傳
- 流程:檔案切片 → Irys 上傳至 Arweave → 取得永久 TXID → 寫入 NFT 的 `tokenURI`
- 目的:深層追思或 AI 訓練時才抓取,平常不讀取以節省流量與算力

### 4.3 節點激勵

- **創世儲存激勵**:家屬一次性支付儲存費(USDC 或法幣) → 平台轉換為 AR 幣進入「儲存捐贈基金」 → 礦工為賺取基金利息,競爭性地證明自己保存了該筆資料
- 此機制保證資料**經濟學上**會被永久保存,不依賴單一平台

---

## 五、運算層:AI 數位分身

### 5.1 視覺生成 — LoRA 微調

從 Arweave 下載逝者的 15-30 張高品質照片 → 使用 BLIP-2 或 LLaVA 自動打標(Captioning) → 以 SDXL 或 Flux 為基礎模型,啟動 LoRA (Low-Rank Adaptation) 訓練 → 訓練參數建議 Rank (Network Dim) 設為 16 或 32,針對 person 類別 → 訓練完成後,輸入「在海邊散步」即可生成該逝者外貌的全新影像。

### 5.2 動態驅動 — LivePortrait / HeyGen

只需數秒逝者影片素材,即可驅動 AI 生成「會說話、有表情」的視訊畫面。

### 5.3 聲音複製 — ElevenLabs / GPT-SoVITS

從影片中抽取音檔 → 訓練聲音模型 → AI 不僅外貌相似,口音、語頻、語調也會與逝者一致。

### 5.4 文字記憶 — RAG

將逝者生前的日記、社交媒體貼文、信件存入向量資料庫(Vector Database) → 使用者提問時,模型先在 Arweave 下載的資料中檢索相關記憶 → 以逝者口吻回答。

### 5.5 算力來源

- 平台自建 GPU 伺服器,或
- 去中心化算力網路(Render、Akash 等)
- **即時運算激勵**:補貼硬體電費與 GPU 損耗,確保互動流暢

---

## 六、使用者互動流程

```
1. 前端展示
   家屬打開網頁 → 讀取 NFT Metadata → 看到精簡生平與大頭照

2. 觸發 AI 互動
   家屬點擊「啟動數位分身(互動追思)」

3. 付費驗證
   家屬支付互動費(USDC 或法幣)
   AI Server 驗證家屬錢包確實持有對應的塔位 NFT

4. 資料抓取
   平台根據 NFT 的 tokenURI → 從 Arweave 抓取該逝者的照片/音檔/文字
   下載至 AI Server 或瀏覽器本地端

5. 模型推理
   ├─ 視覺:Stable Diffusion / Flux + LoRA
   ├─ 動態:LivePortrait
   ├─ 聲音:ElevenLabs / GPT-SoVITS
   └─ 對話:RAG + LLM

6. 互動結束
   快取資料刪除,原始資料永遠留存於 Arweave
```

---

## 七、技術堆疊清單

| 項目 | 技術 / 標準 | 用途 |
|---|---|---|
| 唯一身分 | ERC-721 | 數位塔位的所有權證明 |
| 家族層級 | ERC-6150 | 宗祠式的父子關係 |
| 持有方式 | 一般 EOA 錢包 | MetaMask、Rabby 等 |
| 永久儲存 | Arweave + Irys | 照片、影片、音檔、文字 |
| 視覺生成 | Stable Diffusion / Flux + LoRA | 重現逝者外貌 |
| 自動標註 | BLIP-2 / LLaVA | 訓練資料的圖片描述 |
| 動態驅動 | LivePortrait / HeyGen | 會說話有表情 |
| 聲音複製 | ElevenLabs / GPT-SoVITS | 還原音色語調 |
| 文字記憶 | RAG + 向量資料庫 | 以逝者口吻回答 |
| 付款方式 | USDC / 法幣 | 一次性儲存費 + 算力費 |
| 算力 | 自建 GPU / Render / Akash | 模型推理 |

---

## 八、價值主張

### 數位永生
即使平台 20 年後倒閉,只要 Arweave 還在運行,家屬持有 NFT 錢包,就能找任何懂技術的人用同樣的 TXID 把資料抓回來重現。

### 抗篡改
逝者生平資料寫在不可變的鏈上,無人能隨意修改——這才是對逝者真正的尊重。

### 數據主權
記憶不再「租用」於 Google、Meta,而是真正屬於家屬本人。

### 文化契合
ERC-6150 的層級結構與東方宗祠文化天然契合,鏈上即是家譜。

---

## 九、未來擴展空間

若日後要加入下列功能,可在不破壞既有架構的前提下升級:

- **祭祀基金**:逝者生前指定一筆代幣綁定塔位,後代啟動 AI 互動時自動扣款 → 加掛 ERC-6551
- **回憶資產化**:把單張照片、單段影片做成子 NFT,由塔位 NFT 容器持有 → 加掛 ERC-6551
- **跨鏈部署**:將塔位 NFT 鏡像至多條鏈,進一步降低單鏈失效風險
- **DAO 治理**:家族委員會以 NFT 投票決定塔位的管理權移轉

---

## 十、一句話總結

> 我們正在建立人類史上第一個「主權數位先祖系統」。
> 透過區塊鏈不可篡改性確立家族位階,
> 透過 Arweave 永久儲存封裝遺產,
> 透過生成式 AI 賦予數據生命。
>
> 這不僅是追思,
> 這是在數位荒野中,為每個人建立一座永不熄滅的燈塔。